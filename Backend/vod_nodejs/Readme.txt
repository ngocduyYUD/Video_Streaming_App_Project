- Trong thư mục serverside, dùng lệnh 'node server.js' để chạy server (cần cài nodejs).
Server sẽ stream trên cổng 8000 (đổi cổng khác thì sửa chỗ app.listen)

- Trong thư mục clientside, chạy file client.html bình thường